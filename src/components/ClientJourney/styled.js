import styled from "styled-components";

export const ClientJourneyContainer = styled.div`
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 30px;
    background: #fff;
    border-radius: 10px;
    box-shadow: 0 5px 15px rgba(0,0,0,0.1);
    width: 200px;
    position: fixed;
    bottom: 30px;
    z-index: 1;
`;